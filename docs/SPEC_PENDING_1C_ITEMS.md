# Pending питання до 1С розробника

Чинні (невиконані) специфікації і баги що чекають на 1С-розробника.

**Виконані позиції** → [ARCHIVE_SPECS_RESOLVED.md](./ARCHIVE_SPECS_RESOLVED.md) (Action A `getClientFocus`, Action C `isReserved`, Action 5 `includeAll`, Bug 1 isReserved-sync, Bug 2 checkActivities, clientStats discrepancy).

**✅ Усі pending-позиції закриті.** Action B (`getClientActivationPlan`) доставлено 1С і інтегровано 2026-05-28 (commit `6d4a221`, Hero Card 3 на /clients). ⚠️ Дрібниця: 1С шле `totalInCategory` як **string** ("120"), а не number — обробляємо coerce-ом; нам це поле і так не потрібне (категорії рахуємо самі). Спека нижче лишена для довідки.

---

## Загальне

Нові HTTP-дії додаються до існуючого 1С web-сервісу. Виклик через ту саму endpoint-у що `getClientReport`/`getRegionData` тощо — просто новий `action` у payload. Авторизація — Basic Auth + `X-Service-Token` (як для решти дій).

Формат запиту/відповіді — стандартний:
```json
// Запит
{ "action": "<actionName>", "payload": { ... } }
// Відповідь успіх
{ "status": "success", "data": { ... } }
// Відповідь помилка
{ "status": "error", "message": "..." }
```

---

# Action B: `getClientActivationPlan`

**Призначення:** Повернути **план активації клієнтів по категоріях** для менеджера за заданий період (місяць). Джерело — документ/регістр **«Планування активації бази клієнтів»** у 1С. Менеджер у документі вказує, скільки клієнтів з кожної категорії (Спящий / Потерянный / Без закупок / тощо) він планує активувати протягом місяця. У документі також зберігається довідкове «Кількість клієнтів у категорії» — щоб видно було база vs план.

**Приклад документа у 1С:**
```
Номер: 000000527
Дата: 29.04.2026
Регіон: Харків
Менеджер: Тесленко Марія
Період реєстрації: 01.05.2026 — 31.05.2026

Активності по категоріях:
| N | Категорія клієнта | План | Клієнтів у категорії |
|---|---|---|---|
| 1 | Спящий           | 3    | 36   |
| 2 | Потерянный       | 4    | 208  |
| 3 | Без закупок      | 3    | 148  |
```

## B.1 — Запит

```json
{
  "action": "getClientActivationPlan",
  "payload": {
    "login": "manager.kharkiv@emet.in.ua",
    "period": "2026-05"
  }
}
```

| Параметр | Тип | Обов'язковий | Опис |
|---|---|:---:|---|
| `login` | string | Так | Логін менеджера. Документ прив'язаний до менеджера — повертати лише його план. |
| `period` | string | Так | Місяць у форматі `YYYY-MM`. Шукаємо документ де `Період реєстрації` перетинається з цим місяцем (або точно дорівнює). |

## B.2 — Відповідь

```json
{
  "status": "success",
  "data": {
    "period": "2026-05",
    "documentNumber": "000000527",
    "documentDate": "2026-04-29",
    "periodStart": "2026-05-01",
    "periodEnd": "2026-05-31",
    "region": "Харків",
    "activations": [
      { "category": "Спящий",      "planCount": 3, "totalInCategory": 36  },
      { "category": "Потерянный",  "planCount": 4, "totalInCategory": 208 },
      { "category": "Без закупок", "planCount": 3, "totalInCategory": 148 }
    ]
  }
}
```

### Поля відповіді

| Поле | Тип | Обов'язкове | Опис |
|---|---|:---:|---|
| `period` | string | Так | Як у запиті — `YYYY-MM`. |
| `documentNumber` | string \| null | Так | Номер документа у 1С. `null` якщо документ не заведено. |
| `documentDate` | string \| null | Так | Дата документа `YYYY-MM-DD`. `null` якщо нема. |
| `periodStart` | string \| null | Так | Початок періоду реєстрації `YYYY-MM-DD`. `null` якщо нема. |
| `periodEnd` | string \| null | Так | Кінець періоду реєстрації. |
| `region` | string \| null | Так | Регіон менеджера як у документі. |
| `activations` | ActivationRow[] | Так | Масив рядків з табличної частини документа. Може бути порожній якщо документу нема. |

### Поля `ActivationRow`

| Поле | Тип | Обов'язкове | Опис |
|---|---|:---:|---|
| `category` | string | Так | Категорія клієнта в оригінальному формулюванні з 1С (`Спящий` / `Потерянный` / `Без закупок` / `Активный` / `Новый`). Sales Planning має мапінг RU→UA. |
| `planCount` | number | Так | Скільки клієнтів цієї категорії менеджер планує активувати у періоді. Цілочислове. |
| `totalInCategory` | number | Так | Скільки всього клієнтів цієї категорії у менеджера зараз. Цілочислове. |

## B.3 — Логіка в 1С

1. Знайти **актуальний** документ «Планування активації бази клієнтів»:
   - `Менеджер.Логін == login`
   - `Період реєстрації початок ≤ period_last_day AND Період реєстрації кінець ≥ period_first_day` (перетин з місяцем)
   - Якщо документів кілька (рідкісний випадок) — взяти останній за `Дата`.
2. Якщо документ знайдено:
   - Повернути шапку (`documentNumber`, `documentDate`, `periodStart`, `periodEnd`, `region`)
   - Розгорнути табличну частину «Активності по категоріях» → `activations[]`
3. Якщо документу нема:
   - Повернути `documentNumber=null, …=null, activations=[]` (НЕ помилка).

## B.4 — Edge cases

- Документ є але табличка пуста → `activations: []`, шапка заповнена. Не помилка.
- `period` у майбутньому → шукати все одно (менеджер міг заздалегідь заповнити).
- `period` старший — підтримуємо без обмежень (історія планів цінна).
- Якщо менеджера нема у 1С → `status: error, message: "manager not found"`.

## B.5 — Performance

- Цільова швидкодія: < 200мс.
- Очікувана частота: 1-2 рази на сесію менеджера. Кешуємо на 5хв.

---

## Sales Planning — як використовуватиме (для довідки)

```ts
// src/lib/use-my-clients.ts (extension)
const planPayload = login && month
  ? { login, period: month }
  : null;
const { data: planRes } = useOneCData('getClientActivationPlan', planPayload);

// План: скільки активувати + скільки фактично активовано (порівняння
// з реальними купівлями з categoryы → progress-bar по кожній категорії).
```

UI: блок «План активації» зверху або у sidebar — таблиця 3-4 рядки:
| Категорія | План | Активовано | % виконання |
|---|---|---|---|
| Сплячі | 3 | 2 | 67% |
| Втрачені | 4 | 1 | 25% |
| Без закупок | 3 | 0 | 0% |

Допоможе менеджеру бачити чи виконує план активації.

---

## Контакти

Питання по специфікації — у Telegram або інлайн у документі.

---

_Last updated 2026-05-27._
