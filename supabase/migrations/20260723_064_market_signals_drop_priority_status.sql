-- ============================================================================
-- Migration 064: market_signals — прибрати priority + status (не в регламенті).
-- Created 2026-07-23
-- ============================================================================
--
-- Регламент 4.5 (макет rop-report.html) визначає сигнал як: текст + джерело +
-- кому (→ CPO/CMO) + дедлайн. Пріоритет і статус були додані понад специф —
-- прибираємо, щоб блок був простим полем вводу як у регламенті.
-- CHECK-обмеження (market_signals_priority_chk / _status_chk) знімаються разом
-- зі своїми колонками.
-- ============================================================================

ALTER TABLE market_signals DROP COLUMN IF EXISTS priority;
ALTER TABLE market_signals DROP COLUMN IF EXISTS status;
